
import { secondTherapy } from 'ezspeaky/helpers/second-therapy';
import { module, test } from 'qunit';

module('Unit | Helper | second therapy');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = secondTherapy([42]);
  assert.ok(result);
});

